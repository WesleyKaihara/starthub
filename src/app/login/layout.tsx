import "../../styles/globals.css";

export const metadata = {
  title: "StartHub - Login",
  description:
    "Plataforma para auxiliar na idealização e desenvolvimento de startups",
};

export default function DestaqueLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
